import React from 'react';

export const metadata = {
  title: 'Contact Me',
  description:
    "Get in touch for your web development needs. Specializing in JavaScript, TypeScript, Node, Next.js, React, and Solid, I'm ready to bring your project to life.",
};

const ContactLayout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default ContactLayout;
